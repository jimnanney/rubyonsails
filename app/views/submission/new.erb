<%= simple_form_for Submission.new, :url => :submission do |f| %>
  <%= f.input :title %>

  <div class="row">
    <%= f.label :bio, nil, {
      :class => "form-label col-12 hidden"
    } %>

    <%= f.text_area :text, {
      "data-provide" => "markdown",
      "class" => "form-input col-12 textarea autosize",
      "placeholder" => "Do you think you have what it takes?"
    } %>
  </div>

  <%= f.button :submit %>
<% end %>
